# WhaleClaw Dependency List (Windows)

## Python version
- Requires `Python 3.12+` (recommended `3.12.x`)

## Runtime dependencies (`project.dependencies`)
- `fastapi>=0.115`
- `uvicorn[standard]>=0.34`
- `websockets>=14.0`
- `pydantic>=2.10`
- `pydantic-settings>=2.7`
- `httpx>=0.28`
- `typer>=0.15`
- `rich>=13.9`
- `structlog>=24.4`
- `aiosqlite>=0.20`
- `python-multipart>=0.0.18`
- `cryptography>=43.0`
- `pyyaml>=6.0`
- `playwright>=1.48`
- `lark-oapi>=1.5`

## Dev dependencies (`project.optional-dependencies.dev`)
- `pytest>=8.3`
- `pytest-asyncio>=0.24`
- `pytest-cov>=6.0`
- `ruff>=0.8`
- `pyright>=1.1`
- `pytest-mock>=3.14`
- `httpx[http2]>=0.28`

## Office optional dependencies (`project.optional-dependencies.office`)
- `python-docx>=1.1`
- `openpyxl>=3.1`
- `python-pptx>=1.0`

## Vision optional dependencies (`project.optional-dependencies.vision`)
- `numpy>=1.26`
- `opencv-python>=4.10`

## Recommended install commands
Run in project root:

```powershell
python -m pip install -U pip setuptools wheel
python -m pip install -e ".[dev,office,vision]"
```

If you need Playwright browser runtime:

```powershell
python -m playwright install chromium
```
